# AGENTS.md

本文件约束所有在本仓库中工作的开发代理。适用范围为仓库根目录及全部子目录；若子目录存在更具体的 `AGENTS.md`，以更具体的规则为补充，但不得违背本文件。

## 1. 沟通规则

- 所有面向用户的回复、项目记录、运行说明和新增业务注释使用中文；库名、参数名、公式及代码标识符保留英文。
- 回复先说明结果和当前状态，再说明关键改动、验证证据和下一步。
- 不重复询问能够从仓库、数据目录、运行日志或现有文档中查明的信息。
- 遇到会显著改变技术路线、数据格式或硬件假设的选择时，先说明影响，再请求用户决定。

## 2. 项目目标与路线

当前项目面向园区低速自动驾驶车辆，按可解释、可验证、逐阶段演进的方式建设视觉检测能力。

当前第一阶段固定为：

```text
KITTI image_2 左图 -> CenterNet COCO 2D检测 -> 类别、置信度、二维框
KITTI image_2/image_3 + P2/P3 -> StereoSGBM -> 视差与深度
二维框 + 深度 -> 稳健框内测距 -> 距离、相机坐标、风险提示
```

阶段演进顺序：

1. 跑通并验证 CenterNet 2D检测与双目测距；
2. 使用 KITTI 标签批量评估深度误差；
3. 加入连续帧跟踪、速度与 TTC；
4. 使用园区数据微调类别与检测模型；
5. 再评估双目深度网络、3D检测头或毫米波雷达融合。

不得为了追求榜单指标，在没有说明输入传感器变化的情况下，把单目、双目、激光雷达或多模态结果直接横向比较。

## 3. 开始任务前

- 先阅读根目录 `README.md`、`readme/PROJECT_LOG_CN.md` 和与任务相关的专题文档。
- 先执行 `git status --short`，识别用户已有改动；不得覆盖或回滚无关修改。
- 涉及数据时先检查实际目录、文件数量、左右帧对应关系、图像尺寸和标定文件，不依赖猜测。
- 涉及运行环境时先寻找已有 Conda/Python 环境并检查依赖，避免无必要地重复下载大型包和模型，当前项目使用`clip`环境。
- 修改前明确当前阶段的最小闭环，优先完成一个可运行、可观察、可解释的案例。

## 4. 代码实施规则

- 当前主入口是 `src/stereo_kitti_demo.py`；第一阶段新增能力优先围绕该入口扩展。
- 保持检测、双目深度、目标测距、可视化和结果导出之间的边界清晰，便于以后替换任一模块。
- 新增命令必须提供合理默认值，并能从项目根目录直接执行。
- 输入缺失、标定无效、左右图尺寸不一致、视差不足等情况必须明确失败或返回不可用状态，不得伪造数值。
- 数值输出必须带清晰单位；图像坐标、相机坐标、BEV坐标和不同传感器坐标不得混用。
- 距离估计必须保留有效样本比例或类似质量信息，不能只输出一个没有可信度依据的距离。
- 不引入与当前阶段无关的大型框架、模型或抽象层。
- 新增依赖同步写入 `requirements.txt` 或相关安装文档，并说明实际使用的运行环境。

## 5. 数据与模型管理

- KITTI 数据放在 `data/kitti`，模型权重放在 `models`，运行结果放在 `exp`；这些大文件默认不提交 Git。
- 下载的原始压缩包默认保留，确认用户要求清理前不要删除。
- 解压大型数据前先检查压缩包结构、完整性和磁盘空间；解压后核对文件数量和配对关系。
- `models/ctdet_coco_dla_2x.pth` 是 COCO 预训练的 CenterNet DLA-34 2D检测权重，不得描述成 KITTI、双目或3D训练权重。
- `label_2` 是评估真值，`calib` 是传感器几何标定；不得把标签或点云生成的真值信息泄漏到实际推理输入。
- KITTI `P2/P3` 按帧读取，不使用跨帧平均矩阵替代。

## 6. 验证标准

每次代码改造至少执行与风险相匹配的验证：

- Python 语法或导入检查；
- 相关单元测试；
- `git diff --check`；
- 数据数量、文件名配对和图像尺寸检查；
- 至少一个真实样例的端到端运行；
- 对生成的检测图、视差图等进行视觉检查；
- 对 JSON 中的类别、框、距离、坐标和无效状态进行抽查。

涉及测距精度时：

- 不以单帧或少量样例宣称整体精度；
- 区分 KITTI 标签的3D框位置和 SGBM 得到的可见表面深度；
- 按距离、遮挡、截断和目标尺寸分桶统计误差；
- 同时记录失败率和有效深度比例，不能只统计成功样本。

若因数据、依赖或硬件缺失无法完成端到端验证，必须在项目记录和交付说明中明确列为未验证项。

## 7. 可视化与项目记录

- 每个阶段都应提供直观输出，优先包括检测叠加图、视差/深度图和结构化 JSON。
- 可视化标签应尽量避免重叠，并明确类别、置信度、距离和无效深度。
- 完成实质改造后更新：
  - `README.md`：当前能力、状态、快速运行命令；
  - `readme/PROJECT_LOG_CN.md`：日期、目标、改动、验证结果、已知问题和下一步；
  - 对应专题文档：原理、数据约定、参数和限制。
- 文档中的完成状态必须与实际代码和验证结果一致，不能提前勾选。

## 8. 清理和安全边界

- 可主动清理 `.DS_Store`、`__pycache__`、旧调试缓存和可重复生成的临时输出。
- 清理用户数据、模型、压缩包、标注、运行成果或大量官方源码前，必须先确认范围。
- 优先使用系统废纸篓完成可恢复清理，不使用破坏性的大范围删除命令。
- 原 CenterNet 底层训练/推理代码默认保留；只有确认后续不再需要相应能力时，才删除官方模块。
- 自动驾驶相关结果必须标明教学/验证性质。未经实车标定、延迟测试、场景覆盖和安全冗余验证，不得描述为可直接控制车辆或作为唯一制动依据。

## 9. Git规则

- 未经用户明确要求，不自动提交、推送或创建 Pull Request。
- 提交前展示或说明改动范围，确认没有把数据集、模型权重、运行结果和隐私文件加入版本库。
- 提交信息保持简洁、中文优先，描述实际功能和验证结果。
- 任何地方都不得出现 AI 署名，包括提交作者、共同作者、提交信息、代码注释、文档尾注、PR描述和生成标记。
- 不使用 `Co-authored-by`、`Generated-by` 或其他形式的 AI 标识。
- 不覆盖用户已有提交，不使用破坏性 Git 命令清除用户改动。

## 10. 交付格式

任务完成时至少说明：

1. 已实现的结果；
2. 主要文件入口；
3. 实际执行过的验证及结果；
4. 尚未验证或已知限制；
5. 用户可以直接执行的下一条命令。

不要只汇报“代码已修改”，必须给出能够复核的文件、命令或输出证据。
