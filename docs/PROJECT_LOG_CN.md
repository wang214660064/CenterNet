# 项目记录

## 2026-08-12：收敛为单帧双目3D检测，补齐Gate/Offset诊断

- 工作边界固定为单帧双目3D目标检测；跟踪、速度/TTC、预测、规划和控制不在本仓库优化范围内；
- `src/main.py stereo_ddd ... --test`在原KITTI预测外新增`stereo_detection_diagnostics.json`，记录每个候选的Direct深度、SGBM深度、质量、几何/残差Offset、Gate、不确定性和回退原因；
- `src/tools/analyze_stereo_errors.py`新增目标级诊断关联，输出Gate选择正确率、Gate regret、Direct/SGBM/融合深度误差，并按SGBM质量和回退原因分桶；
- 禁止使用缺少右图和SGBM输入的`src/test.py`评估`stereo_ddd`，避免静默退化为直接深度；
- macOS本地无法运行仓库内置的Linux KITTI官方评估器时，明确跳过AP计算并保留预测、真值和诊断产物；Linux服务器行为不变；
- 8项相关单元测试通过；使用`clip`环境、v5第26轮最佳权重和真实KITTI帧`000002`完成CPU端到端前向，诊断JSON包含1个有效Car候选；
- 单帧只用于验证链路，不作性能结论。下一步在Linux服务器对固定400帧验证集重新推理，再根据Gate regret和分距离结果决定是否仅优化Gate标定或Offset残差，不预先增加新网络模块。
- Linux服务器完成固定400帧复验，Car Moderate 3D AP_R40为`42.23`，与历史v5的`42.24`一致；诊断对748个匹配目标的覆盖率为100%；
- Moderate目标最终深度MAE为`0.847m`，直接深度为`1.441m`；原始中心SGBM经Offset修正后，在717个有效目标上MAE由`1.646m`降至`1.074m`，74.62%的目标获得改善；
- Gate软融合在有效目标上的MAE为`0.817m`，优于直接深度`1.437m`、修正SGBM`1.074m`和0.5阈值硬切换`0.930m`；因此Gate与Offset都不应取消；
- Gate对明显优劣候选的选择正确率为72.31%，候选Oracle MAE为`0.617m`，说明仍有约`0.23m`的目标级融合标定空间；
- 主要短板是15～30m Gate选择率仅68.26%，以及质量0.5～0.8目标中融合MAE`1.310m`高于直接深度`1.222m`；30～50m的Offset改善率也仅59.19%，但最终融合仍显著优于直接深度；
- 下一轮候选固定为“只校准Gate，不改网结构”：重点抑制中等SGBM质量时的过度融合，并对15～30m错选样本做困难样本加权；先做单变量A/B，不引入DFL、新注意力或全头解冻。

## 2026-08-11：新增YOLOv8风格总览框架SVG

- 参考YOLOv8网络架构图风格，新增总览框架图`docs/architecture/11_yolov8_style_framework.svg`；
- 复核并修正框架逻辑：DLA只接收左图，SGBM由左右图和P2/P3并行产生；拆分`z_sgbm`、`quality`、`target_features`和各检测头到融合/解码的独立连接；
- 图中分别展示DLA-34骨干、CenterNet检测头、SGBM质量编码、质量感知融合、Geometry Offset v4、Depth Fusion Gate和Projected Center v5；
- 通过颜色区分输入、图像骨干、检测头、双目几何、融合创新模块和解码输出；
- 底部保留连线语义、园区距离策略、验证指标与教学原型安全边界；
- 新增生成脚本`src/tools/generate_yolov8_style_architecture.py`，使用Python标准库，无需额外绘图依赖；
- 使用PNG整图渲染进行视觉检查，并验证SVG无XML解析错误；
- 同步更新`docs/architecture/README_CN.md`版本列表和重新生成说明。

## 2026-08-10：Dimension v6b相对尺寸损失

- 新增`--train_dimension_only`，从v5最佳权重初始化，只训练已有`dim`尺寸头；
- 原始米制`dim_loss`在v6b脚本中关闭，新增相对尺寸Smooth L1：每一维误差除以对应真实尺寸，避免车辆长度主导损失；
- 新增参数`dimension_aware_weight`和`dimension_aware_beta`，默认权重为0以保证旧实验兼容；v6b分别设置为`1.0/0.1`；
- 训练时终端新增`dima`指标，训练曲线新增`dimension_aware_loss`；
- 修复零权重损失的统计类型：关闭某项损失时仍返回独立零值Tensor，避免进度条调用`.mean()`报错；
- 34项自动化测试通过；真实KITTI验证帧完成前向和反向传播，原始`dim_loss`为0，新相对尺寸损失为`0.02727`，只有`dim`头4个参数张量获得梯度；
- 新训练入口为`experiments/stereo_ddd_dimension_v6b.sh`，尚未启动正式训练，必须与v5在同一400帧验证集比较。
- 服务器训练在第6轮早停；保存的最佳权重为第1轮，后续最低验证损失虽为`4.80674`，但相对改善未达到早停阈值`0.002`；
- 在`project2000`同一400帧验证集完成正式评估，Car Moderate BEV/3D AP_R40为`45.75/42.18`，低于v5的`45.87/42.24`；
- 尺寸MAE由v5的`0.15987m`微升至`0.16041m`，相对尺寸误差由`6.17%`升至`6.22%`；平均3D IoU由`0.53321`降至`0.53299`，`IoU≥0.7`比例由`26.07%`降至`25.53%`；
- 仅0～15m尺寸MAE由`0.14362m`降至`0.14230m`，15～50m均未改善；结论是相对尺寸损失不替代v5，后续仍优先解决深度与投影中心误差。

## 2026-08-10：Projected Center v7尺度归一化重叠代理损失

- v6a正式评估未形成稳定收益，因此v7从v5最佳权重重新开始，不继承v6a权重；
- 保留v5像素投影中心Smooth L1，关闭v6a米制XY损失；
- 根据KITTI真值尺寸`[h,w,l]`和`rotation_y`计算车辆在相机X方向的有效宽度，并用车辆高度归一化纵向误差；
- 新增可导中心重叠代理损失；完全不重叠时叠加归一化Huber项，避免困难目标出现零梯度；
- 新损失默认权重为0，保证旧实验兼容；v7脚本设置`proj_center_iou_weight=0.2`、`proj_center_iou_beta=0.1`；
- 继续使用`--train_projected_center_only`，深度、gate、尺寸、朝向、2D头、骨干与BatchNorm全部冻结；
- 新增训练入口`experiments/stereo_ddd_projected_center_v7_iou.sh`，正式结果必须与v5在同一400帧验证集做A/B比较；
- 30项自动化测试通过，Python与Shell语法检查通过；
- 使用本地v5第26轮最佳权重和真实KITTI验证帧完成前向、损失及反向传播：有效目标尺度为`1.41～4.37m`，v7代理损失为`0.2356`，只有`proj_center_offset`头的4个参数张量获得梯度。
- 服务器完成25轮训练并早停；按`early_stopping_min_delta=0.002`保存的最佳权重为第20轮，验证总损失由`4.8080`降至`4.7757`，代理损失由`0.22394`降至`0.22290`；
- 在`project2000`同一400帧验证集完成正式评估，Car AP_R40（Easy/Moderate/Hard）：BEV为`57.89/45.86/38.77`，3D为`53.22/41.99/35.09`；
- 相比v5，Car Moderate BEV AP仅下降`0.016`个百分点，而3D AP下降`0.253`个百分点；平均中心XY误差由`0.28344m`变为`0.28391m`，平均3D IoU由`0.53321`变为`0.53276`；
- 分距离看，0～15m的平均3D IoU由`0.59936`升至`0.59973`，且`IoU≥0.7`比例由`37.43%`升至`38.50%`；15～30m、30～50m平均3D IoU分别下降至`0.55398/0.46676`，均未形成改善；50m以上只有3个目标，不作为结论依据；
- 遮挡分桶的2D召回完全不变，平均3D IoU变化均小于`0.002`，说明v7没有改善遮挡或检测问题；
- 误差反事实依然显示深度为首要瓶颈：替换真值深度可令平均3D IoU增加`0.1153`，替换中心XY仅增加`0.0473`，尺寸和朝向分别为`0.0114/0.0188`；
- 视觉抽查中，`004294`常规车辆框与BEV总体贴合，`000102`仍有明显中心偏移和漏匹配；结论是v7不采用，当前最佳模型继续为v5。

## 2026-08-10：补充全版本SVG架构图

- 新增项目演进总览，以及阶段1、Stereo DDD基线、Fusion Gate v2、Campus Gate v3、Geometry Offset v4、Projected Center v5、v6a和v7共9张SVG；
- 统一输入、神经网络、传统几何、融合、输出、损失和冻结模块的颜色与图例；
- 每张图明确数据流、版本增量、训练梯度、冻结边界、当前状态和初学者一句话；
- LightStereo和3D属性头全解冻作为未进入主线的消融，只出现在演进总览旁支；
- 新增`src/tools/generate_architecture_svgs.py`，使用Python标准库可重复生成全部图；
- SVG包含`title`、`desc`和无障碍标识，统一使用`1600×1000`画布，便于GitHub、浏览器和文档嵌入。

## 2026-08-10：Projected Center v6a相机XY一致性损失

- 保留v5的投影中心像素Smooth L1，新增相机坐标`x/y`一致性损失；
- 使用每帧真实`P2`和输出到原图的逆仿射矩阵，将预测投影中心反投影到相机坐标；
- 最终融合深度在进入新损失前执行`detach()`，并继续使用`--train_projected_center_only`，确保深度、gate、尺寸、朝向和骨干网络不被修改；
- 新增参数`proj_center_xy_weight`和`proj_center_xy_beta`，默认权重为0以兼容旧实验，v6a脚本分别设置为`1.0`和`0.2m`；
- 新训练入口为`experiments/stereo_ddd_projected_center_v6a.sh`，从服务器v5最佳权重继续训练，学习率为`2e-5`；
- 全部27项自动化测试通过，Python语法和Shell脚本检查通过；
- 真实KITTI样本中，GT投影/反投影的相机XY平均误差为`0.00019m`，新损失为有限值；反向传播时投影中心头梯度和为`18.63`，其他分支收到梯度的参数数为0；
- 服务器完成30轮训练；按`early_stopping_min_delta=0.002`保存的最佳权重为第25轮，验证像素投影损失由约`1.367`降至`1.334`，XY损失由`0.1904`降至`0.1894`；
- 在`project2000`同一400帧验证集完成正式评估，Car AP_R40（Easy/Moderate/Hard）：BEV为`57.90/45.85/38.74`，3D为`54.80/42.29/35.42`；
- 相比v5，Car Moderate 3D AP仅增加`0.045`个百分点，BEV AP下降`0.027`个百分点；中心XY MAE由`0.28344m`降至`0.28336m`，平均3D IoU由`0.53321`降至`0.53310`，`IoU≥0.7`比例由`26.07%`降至`25.80%`；
- 分距离看，0～15m中心XY误差降低约`1.0mm`，15～30m增加约`0.39mm`，30～50m基本不变；这些变化不足以证明米制XY损失带来稳定收益；
- 误差反事实仍显示深度为首要瓶颈：替换真值深度可令平均3D IoU增加`0.1154`，替换中心XY增加`0.0469`，替换尺寸和朝向分别增加`0.0113/0.0188`；
- 单帧抽查中，`004294`常规车辆框与BEV总体贴合，`000102`出现明显中心偏移和漏匹配；结论是v6a未解决困难长尾样本，当前继续保留v5作为最佳模型；
- 最佳权重、训练曲线、AP、逐目标误差明细和两帧可视化已下载到本地`exp/stereo_ddd`留档。

## 2026-08-09：Projected Center v5正式评估

- 首次前台训练完成11轮后为保护SSH断线场景，从第11轮最佳权重在`screen`后台续训30轮；
- 续训最佳权重为第26轮，验证`proj_center_loss`由1.653持续降至1.368，第27～30轮在`1e-6`学习率下进入平台，无明显过拟合反弹；
- `project2000` 400帧Car AP_R40（Easy/Moderate/Hard）：BEV为`57.99/45.87/38.70`，3D为`54.81/42.24/35.37`；
- 相比v4，Car Moderate BEV AP由43.44提升到45.87（+2.43），3D AP由30.36提升到42.24（+11.89）；
- 2D AP、AOS、深度MAE 0.847m和尺寸MAE 0.160m与v4保持一致，说明收益不是由其他头漂移造成；
- Car Moderate中心`x/y`平面误差由0.324m降至0.283m，横向`x` MAE由0.285m降至0.252m，垂直`y` MAE由0.105m降至0.081m；
- 平均3D IoU由0.491升至0.533，IoU达到0.7的匹配目标比例由12.30%升至26.07%；
- 0～15m改善最明显：中心`x/y`误差由0.361m降至0.247m，平均3D IoU由0.486升至0.599；15～30m平均3D IoU由0.524升至0.555；30～50m基本持平；
- 修正中心`x/y`的反事实IoU增益由0.087降至0.047，说明新头已消化了约一半原中心定位缺口；
- 单帧`004294`对比中，关键车辆的3D IoU由0.394升至0.779，中心误差由0.554m降至0.095m；可视化仍显示边缘截断车辆和远距离目标存在框偏移；
- 当前最佳权重为服务器`exp/stereo_ddd/stereo_project2000_projected_center_v5_continue_e11/model_best.pth`，评估产物位于`exp/stereo_ddd/stereo_project2000_projected_center_v5_eval`。

## 2026-08-09：增加3D中心投影偏移头

- 保留CenterNet原二维框中心热力图，新增2通道`proj_center_offset`头；
- 使用KITTI当帧`P2`将3D框几何中心投影到图像，监督其相对于2D框中心的偏移；
- 2D框解码仍使用原中心，相机坐标和`rotation_y`恢复使用修正后的投影中心；
- 新损失采用Smooth L1，并沿用`2.0/1.5/0.5/0.0`园区距离权重；
- 超过64个特征格的极端画外偏移暂不参与监督，推理使用同样的向量限幅；保留训练/验证98.66%/98.52%的目标；
- 新头最后一层零初始化，旧Geometry Offset v4权重加载时偏移为0，不改变原推理结果；
- 新增`--train_projected_center_only`，单变量实验只更新148,226个新头参数；
- 误差分析新增相机坐标`x/y`误差、中心平面距离及修正`x/y`的反事实3D IoU；
- 新训练入口为`experiments/stereo_ddd_projected_center_v5.sh`，从v4最佳权重初始化；
- 26项项目测试通过；真实KITTI样本生成有效投影中心监督，完成前向、总损失及反向传播，新头梯度正常；
- 尚未启动正式训练，尚不能判定Projected Center v5是否提升AP。

## 2026-08-09：3D属性头解冻阶段正式评估

- 服务器实际使用`batch_size=16`，训练在第6轮早停，最佳权重为第2轮，最低验证损失3.3911；
- 在`project2000`的400帧验证集完成推理、KITTI AP_R40和误差分解；
- Car Moderate BEV/3D AP_R40为42.32/29.44，低于冻结3D头的Geometry Offset v4的43.44/30.36；
- 最终深度MAE由0.847m小幅降至0.844m，尺寸MAE由0.160m降至0.158m，朝向误差由21.58°降至21.50°，但平均3D IoU由0.4915微降至0.4909；
- 0～15m、15～30m、30～50m深度MAE分别为0.437m、0.750m、1.214m，与v4基本相同；
- 2D AP保持不变，说明冻结骨干和二维头生效；AOS只有极小变化；
- 结论：平均回归误差的微小改善没有转化为IoU阈值下的AP提升，当前不采用解冻权重，继续使用`stereo_project2000_geometry_offset_v4/model_best.pth`。
- 评估结果位于服务器`exp/stereo_ddd/stereo_project2000_geometry_offset_3d_heads_v5_eval`。

## 2026-08-09：Geometry Offset v4正式评估

- 训练在第12轮早停，最佳权重为第7轮，最低验证损失3.3958；
- 使用`project2000`的400帧验证集完成推理、KITTI AP_R40和误差分解；
- Car AP_R40（Easy/Moderate/Hard）：BEV为53.18/43.44/36.74，3D为36.92/30.36/26.30；
- 相比v3，Car Moderate BEV AP由41.09提升到43.44，3D AP由28.92提升到30.36；
- Car Moderate匹配目标的最终深度MAE由0.90m降至0.85m，优于原始SGBM的比例由69.80%提高到72.89%；
- 分距离深度MAE为0～15m 0.44m、15～30m 0.76m、30～50m 1.21m；30～50m较v3的1.35m改善最明显；
- 2D AP和AOS与v3完全一致，说明提升来自深度与几何修正，而不是检测头变化；
- 结论：Geometry Offset v4通过正式验证，可替代v3作为当前项目最佳双目3D基线。服务器评估结果位于`exp/stereo_ddd/stereo_project2000_geometry_offset_v4_eval`。

## 2026-08-07：双目第一阶段代码完成

目标：在不训练新 3D 模型的情况下，先建立可以解释和逐段验证的园区低速车感知基线。

已完成：

- 将根 README 从原始论文介绍收束为本项目入口、状态与安全边界；
- 新增 `src/stereo_kitti_demo.py`；
- 使用 COCO CenterNet 检测人、自行车、汽车、摩托车、公交车、卡车、交通灯和停止标志；
- 使用 KITTI `image_2/image_3` 和逐帧 `P2/P3` 运行 StereoSGBM；
- 从检测框中下部中心区域提取有效深度，使用中位数和 MAD 过滤离群点；
- 输出检测叠加图、彩色视差图与结构化 JSON；
- 新增标定/深度几何单元测试；
- 删除 `src/cache/debug` 中十张旧调试图，并清理 `.DS_Store`、`__pycache__` 和旧 demo 输出；这些项目已移入系统废纸篓，可恢复；
- 保留原 CenterNet `src/lib` 训练/推理底层，避免过早破坏后续微调能力。

验证结果：

- 合成标定参数的基线与深度公式测试通过；
- 当前 KITTI `000008` 标定解析得到 `fx=721.5377 px`、`baseline=0.5327 m`；
- Python 语法检查与 `git diff --check` 通过；
- 初次检查时默认 Python 缺少 OpenCV/PyTorch，后续确认 Conda `base` 环境已包含两者。

下一步：

1. 检查 `training/image_3` 为 7481 张、`testing/image_3` 为 7518 张；
2. 安装并确认 CenterNet 原生扩展、OpenCV 和 PyTorch 可用；
3. 先运行 `000008`，检查左右图是否同尺寸、视差方向是否正确；
4. 将双目距离与 `label_2` 的 `z` 分桶比较，记录 5 m、10 m、20 m、40 m 内误差；
5. 根据园区目标大小调整 SGBM 和框内采样区域。

## 2026-08-07：右图接入和首次端到端验证

- `data_object_image_3.zip` 完整性校验通过，原压缩包保留；
- 解压得到训练右图 7481 张、测试右图 7518 张；
- 训练集左右文件名交集为 7481，对应关系完整；
- 安装旧 CenterNet 缺失的轻量依赖 `progress`、`easydict`；
- 使用 Conda `base` 环境和 CPU 成功运行 `000008`；
- CenterNet 输出 12 个候选目标，其中包括 11 个汽车候选和 1 个低置信度交通灯候选；
- 生成检测叠加图、视差图和结构化结果到 `exp/stereo_stage1`。

首次结果与该帧六个 Car 标签按二维框匹配后，SGBM 表面深度分别为 `4.13、6.30、5.03、12.84、33.07、19.28 m`，标签 Z 为 `3.68、7.86、6.15、14.44、33.20、19.96 m`。绝对误差分别约为 `0.45、1.56、1.12、1.60、0.13、0.68 m`。这只是单帧链路检查：标签 Z 表示三维框位置，而框内视差更接近可见表面，不能把这六个值当成正式精度结论。

当前待办缩减为：在 3DOP 验证集上批量统计误差，并处理远处小框、DontCare 区域和低置信度重复候选。

## 2026-08-07：建立仓库级开发约束

- 新增根目录 `AGENTS.md`，适用于整个项目；
- 固化中文沟通、问号进度检查、先检查工作区再修改等协作规则；
- 明确当前双目第一阶段及后续演进顺序，避免无依据地切换传感器路线；
- 明确数据、模型、标定、结果目录及禁止真值泄漏规则；
- 建立语法、单元测试、真实样例、视觉检查和误差统计等验证门槛；
- 约束清理操作保持可恢复，Git 操作不自动提交且任何位置不得包含 AI 署名。

## 2026-08-07：启动 MonoFlex + SGBM 第二阶段

- 根据用户选择，第二阶段不复现 Stereo CenterNet 十分支结构，改为参考 MonoFlex 的3D属性头和多深度融合；
- 保留 SGBM 作为可解释的双目几何深度候选，并计划学习 SGBM 到3D框中心的残差与不确定性；
- 新增 `src/tools/evaluate_stereo_depth.py`，使用真值二维框隔离检测误差，批量评估SGBM；
- 使用 `clip` 环境完成全部 3769 帧 3DOP 验证集评测；
- 共评估17554个目标，16993个有效，有效率96.80%；
- 整体 MAE 2.68m、Median AE 1.40m、RMSE 4.77m、平均相对误差14.32%；
- 无遮挡目标 MAE 1.36m，遮挡等级2目标 MAE 6.32m，证明需要质量门控和不确定性融合；
- 新增 `readme/MONOFLEX_SGBM_STAGE2_CN.md`，记录模型结构、融合方式、实施顺序和验收指标；
- 下一步生成中心偏移、3D尺寸、朝向、10关键点和SGBM残差等训练目标并逐帧可视化。

深度修正定义进一步固定为 `z_final = z_sgbm + depth_offset`：

- 新增 `StereoDepthOffsetHead`，融合图像特征、归一化SGBM深度和SGBM质量；
- 同时输出 `depth_offset` 与 `depth_log_variance`；
- 新增带有效掩码的 Laplace 不确定性损失，SGBM无效区域不参与残差训练；
- 批量评测明细新增 `offset_target_m = z_label - z_sgbm`、深度MAD和IQR质量字段；
- offset由训练集监督学习，禁止使用验证集平均误差作为固定补偿。

## 2026-08-07：完成Stereo DDD模型训练闭环

- 新增 `stereo_ddd` 任务并注册数据集、模型和训练器；
- 数据集在线计算SGBM，生成下采样深度图、质量图、offset标签和有效掩码；
- 新增 `StereoDLASeg`，DLA-34图像特征与SGBM深度/质量在offset分支融合；
- 保留原 `hm/wh/reg/dep/dim/rot` 六个输出，新增 `depth_offset/depth_log_variance`；
- 最终深度使用 `z_sgbm + depth_offset`，质量差或不确定性过大时回退到直接深度；
- 新增 `StereoDddLoss`，总损失加入带掩码的Laplace offset不确定性损失；
- 新增模型结构打印工具和 `experiments/stereo_ddd_3dop.sh` 训练脚本；
- `clip` 环境补充安装 `pycocotools`，并写入requirements；
- 实测模型参数量21,358,237，8个输出头形状正确；
- 真实KITTI样本完成前向与反向传播，offset层平均梯度绝对值为0.0889；
- `src/main.py stereo_ddd` 完成1次训练迭代；当前CPU单迭代约24秒，不适合直接完成70轮训练；
- 修复日志复制命令对包含空格的项目路径不兼容问题。
- 将旧版PyTorch损失参数更新为 `reduction='sum'`，消除弃用警告；
- 非CUDA环境关闭DataLoader的 `pin_memory`，避免无意义警告。

## 2026-08-07：增加可观察的训练进度与成果

- 将旧式终端进度替换为动态 `tqdm` 进度条；
- 每个批次展示显存、总损失、热力图、深度、尺寸、朝向和SGBM offset损失；
- 同时展示当前批次有效目标数和网络输入尺寸，便于发现空标注或尺寸错误；
- 每轮自动写入 `results.csv` 和 `training_summary.json`；
- 每轮自动生成 `results.png`，分面观察总损失、深度、二维检测和三维属性收敛趋势；
- 保留 TensorBoard（环境安装 `tensorboardX` 时启用）、文本日志和权重文件；
- 新增 `--no_progress_bar`，适配日志重定向和CI运行。

验证：使用真实KITTI数据完成2轮、每轮1次迭代的CPU冒烟训练；成功生成CSV、JSON、2080×1280训练曲线图和约241MB的最近权重，4项自动化测试全部通过。该短训练只验证链路和展示效果，不代表模型已经收敛。

## 2026-08-07：服务器训练与早停

- RTX 4090上采用 `batch_size=4`、训练加载进程8、验证加载进程8；
- 首次正式训练在第10轮取得最低验证损失3.624，之后训练损失继续下降但验证损失升高；
- 第33轮人工停止，保留约82MB的最佳模型和第32轮完整训练检查点；
- 新增早停参数，默认训练脚本连续2次验证未改善至少0.01时停止；
- 旧DCNv2编译扩展不再需要，当前可变形卷积由 `torchvision` 提供。
- 修复KITTI评测程序的路径和验证集真值准备流程；
- 新增 `kitti_ap_r40.json`，从41点PR曲线按官方R40口径计算2D、AOS、BEV和3D AP。
- 使用第10轮 `model_best.pth` 完成3769帧3DOP验证集评估；
- Car AP_R40（Easy/Moderate/Hard）：2D为86.17/77.56/69.32，BEV为56.93/44.65/35.18，3D为38.22/30.88/24.38；
- 评估原始文件保存在 `exp/stereo_ddd/stereo_sgbm_offset_eval`，训练成果仅用于教学与离线验证，尚未达到实车部署标准。

## 2026-08-09：补充单帧3D预测可视化

- 新增 `src/tools/visualize_stereo_prediction.py`，可按KITTI帧号加载训练好的Stereo DDD权重；
- 复用训练验证阶段完全相同的数据预处理、SGBM融合、解码和坐标还原逻辑；
- 输出预测3D框、真值3D框、热力图、BEV预测/真值对比和综合视图；
- 输入帧必须属于3DOP验证集，找不到时明确报错，不自动换用其他帧。

## 2026-08-09：分解Car 3D AP误差来源

- 新增 `src/tools/analyze_stereo_errors.py`，读取完整评估结果和SGBM逐目标记录，无需重复模型推理；
- Car Moderate条件目标二维匹配率95.40%，匹配目标平均2D IoU为0.802，说明二维检测不是当前首要瓶颈；
- 最终深度MAE为0.98m，SGBM基线MAE为1.45m，最终深度在71.52%的有效目标上优于SGBM；
- 30～50m最终深度MAE升至1.48m，且只有51.38%的目标优于SGBM，是园区关注距离内最明显的深度退化区间；
- 遮挡等级2的二维匹配率降至65.65%，深度MAE为2.39m、朝向误差47.22°，是Hard指标的主要损失来源；
- 反事实替换真值后，深度可使平均3D IoU提升0.112，朝向提升0.017，尺寸没有稳定净提升；当前优化优先级为深度融合、遮挡检测、朝向，最后才是尺寸。

## 2026-08-09：实施距离感知SGBM offset门控

- SGBM质量图改为31×31局部有效比例，完全由双目图像计算，移除真值框中心对模型质量输入的改写；
- offset同时采用8m绝对上限、SGBM深度15%相对上限和2m近距离最小上限；
- 30m以上把最低质量提高至0.8、最大预测标准差收紧至3m，条件不足时回退网络直接深度；
- 新增门控单元测试，全部8项自动化测试通过；
- 使用真实KITTI数据完成单帧推理可视化和1次训练反向传播，训练损失33.6642，闭环正常；
- 旧权重使用的是旧质量图语义，只用于兼容性冒烟测试；新机制的精度结论必须重新训练后给出。
- 运行设备改为默认自动选择CUDA，无CUDA时自动回退CPU，日常命令不再要求填写GPU参数；
- 新实验加载旧第10轮 `model_best.pth` 作为参数初始化，以 `5e-5` 学习率从第1轮重新训练；不使用 `--resume`，因此不会继承旧优化器、学习率状态或训练轮次。
- 复核早停缩进报告：原指标判断的 `else` 实际配对正确，但与验证周期的外层 `else` 容易混淆；现已提取早停状态函数，并将 `model_last` 改为每轮统一保存；
- 新增回归测试，明确验证指标改善时计数清零，未达到最小改善量时才增加计数。

## 2026-08-09：增加2000帧快速迭代划分

- 从原3DOP训练集固定随机抽取1600帧、从原验证集抽取400帧，保持8:2且避免训练/验证交叉；
- 新增 `create_kitti_quick_split.py`，使用固定种子317，可重复生成 `quick2000` 标注；
- 新增快速训练脚本，每轮完成后验证一次，最多30轮，连续3轮验证未改善时早停；
- 快速子集只用于模型改造筛选，最终AP和分桶误差仍以完整3DOP验证集为准。
- 首次quick2000全量微调在第3轮取得最低验证损失3.8147，第6轮因连续3次未改善早停；训练损失继续下降到1.7292，确认小数据集过拟合；
- 验证热力图损失同步恶化，说明全量更新破坏了已有检测能力；快速实验改为冻结骨干和常规检测头，只训练双目融合、offset和不确定性分支；
- offset-only实验学习率降为1e-5、早停耐心提高到5轮，冻结模块保持eval以避免BatchNorm统计量漂移。
- offset-only复核显示验证offset损失前6轮下降约15.5%后进入平台，说明分支有效但Laplace损失与原始异常目标限制了继续改善；
- 新增 `huber` offset模式：标签按2m最小、深度15%和8m绝对上限裁剪，与推理限幅保持一致；
- Huber负责offset回归，不确定性改为0.05权重的独立校准项，避免通过方差项得到难以解释的负损失；新实验固定为 `stereo_quick_offset_huber_v1`。

## 2026-08-09：project2000设为项目正式数据集

- 固定沿用已经验证的2000帧，不重新抽样：1600帧正式训练、400帧正式验证，交集为0；
- 正式名称改为 `project2000`，默认 `kitti_split`、训练脚本、AP评估和分桶分析均使用该划分；
- 原3DOP划分及结果只作为历史资料保留，不删除，但不再作为本项目默认训练和验收口径；
- 左右图继续引用KITTI原始图像目录，不重复复制大文件；正式帧清单和COCO标注独立保存在 `ImageSets_project2000` 与 `annotations/kitti_project2000_*.json`。

## 2026-08-09：Stereo Fusion Gate v2网络改造

- 新增可学习`depth_gate`，连续融合SGBM修正深度和网络直接深度；
- 保留SGBM有效性、质量、不确定性和offset限幅作为强制安全边界；
- 增加1/4与1/8双尺度融合，兼顾远距离小目标细节和较大范围上下文；
- SGBM质量编码器同时使用归一化深度、有效比例、局部离散度和深度梯度；
- 按预测2D框大小自适应选择3×3、7×7和15×15目标邻域，不再只依赖中心单像素；
- 融合层加入ECA轻量通道注意力；
- 新增gate BCE损失和融合深度Huber损失，训练真值只用于生成监督，不进入推理输入；
- 总参数量由约2136万增加到2268万，`train_stereo_only`时训练约206万参数；
- 旧最佳模型可初始化DLA和常规检测头，新增融合模块重新初始化，禁止使用`--resume`继承旧优化器。

## 2026-08-09：园区距离分层与Gate v3

- 根据400帧正式验证结果，将0～30m定义为核心3D范围、30～50m定义为远距预警、50m以上定义为2D观察；
- 双目offset、gate和融合深度损失权重依次设为2.0、1.5、0.5、0.0，不删除远距离2D训练目标；
- gate由普通BCE改为带误差代价的Focal Loss，`gamma=2`、`alpha=0.5`；
- 两种候选深度误差差小于0.2m时忽略gate分类，避免不稳定硬标签；
- 误差差越大的错误选择获得越高权重，最大附加代价为4；
- 50m以上强制禁用SGBM修正，业务JSON标为`2d_only`，但不改写KITTI官方评估输出；
- 下一实验从Fusion Gate v2第14轮最佳权重初始化，只训练双目分支，不全量解冻。

# 2026-08-09：评估LightStereo候选并继续使用SGBM

## 目标

- 使用LightStereo替换SGBM做单变量A/B，暂不改动CenterNet 3D检测头；
- 避免每个epoch重复执行立体网络，固定project2000视差输入以加快迭代。

## 改动

- 临时接入官方LightStereo-S KITTI权重生成验证集视差，用于同口径A/B；
- 对比完成后按项目路线决策移除LightStereo训练入口、缓存后端和额外依赖，主链路继续只使用SGBM；

## 当前验证与限制

- 已完成本地CPU官方权重推理与400帧验证，不再安排LightStereo服务器训练；
- OpenStereo及预训练权重许可需在商业使用前单独复核；当前成果仅用于教学与离线技术验证。

## 本地project2000验证集深度A/B

- 使用官方`LightStereo-S-KITTI.ckpt`，在本地CPU为project2000全部400帧验证图生成视差缓存，完整性检查为400/400；
- 评估统一使用1821个KITTI真值二维框，隔离2D检测误差；
- SGBM：有效率96.49%，MAE 2.65m，中位绝对误差1.36m，RMSE 4.91m；
- LightStereo-S：有效率100%，MAE 2.84m，中位绝对误差1.54m，RMSE 5.20m；
- 0～15m、15～30m、30～50m的LightStereo MAE分别为1.44m、3.22m、3.38m，均差于SGBM的1.32m、2.95m、2.86m；
- 50m以上LightStereo MAE为4.96m，优于SGBM的5.34m，但该距离不属于园区核心3D控制范围；
- 结论：LightStereo提高了稠密深度有效率，但当前KITTI-S权重不能直接替换园区0～50m的SGBM主后端。下一步应优先分析系统性估近偏差，或将LightStereo作为SGBM无效区域的候选补全源。

## 2026-08-09：Geometry Offset v4

- 项目深度来源固定回SGBM，不再保留LightStereo训练入口和依赖；
- 将总offset拆成尺寸/朝向几何先验与学习残差，几何公式同时考虑3D长度、宽度和观察角；
- 新增可学习几何门控，并乘以SGBM质量，使遮挡和低质量区域减少使用几何先验；
- `dim`与`rot`进入几何分支前执行`detach()`，防止offset损失改变尺寸与朝向预测；
- `--train_stereo_only`已纳入几何门控参数，继续冻结骨干和常规3D头；
- 训练终端新增几何offset均值`geo`和学习残差均值`res`；
- 训练脚本更新为`stereo_project2000_geometry_offset_v4`，默认从Gate v3最佳权重初始化并从第1轮训练。

验证：20项项目测试通过；真实KITTI样本完成完整前向、损失和反向传播，几何门控梯度正常，尺寸/朝向头保持冻结。
